.Dashboard
  center(column)
  height 100vh
  min-height 500px

  .content
    center(column)
    margin-top: pxToRem(94)

  .projectList
    @extend h1
    display flex
    flex-direction column
    flex-grow 0
    list-style none
    position relative
