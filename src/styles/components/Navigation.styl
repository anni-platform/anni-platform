.Navigation
  align-items center
  background: $paper
  border-bottom 1px solid $concrete
  display flex
  justify-content space-between
  padding $md $lg
  position absolute
  top: 0
  width: 100%
  z-index: 999

  .NavigationItems
    align-items: center
    display: flex
