import styled from "styled-components";
import { PAPER } from "./Variables";

export const ProjectList = styled.div`
  background: ${PAPER};
`;


//
// import {ProjecItem, PreviewImage, ProjectDetails, ProjectControls, Paragraph } from 'styled';
