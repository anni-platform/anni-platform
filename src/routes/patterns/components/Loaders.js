import React from "react";

import { Card, Loader } from "styled";

const Loaders = () => (
  <Card className="Loaders">
    <Loader center />
    <h4>Loader</h4>
  </Card>
);

export default Loaders;
